###############################################
# MediPredict Environment Variables (Example)
# Copy this file to `.env` and adjust values.
# The real `.env` must remain ignored by Git.
###############################################

# =====================
# Client (Next.js)
# =====================
# The base URL of the backend API the Next.js app proxies to.
# Example for local development:
NEXT_PUBLIC_SERVER_URL=http://localhost:3001

# =====================
# Server (Express)
# =====================
# Port for the Express server (default 3001 if unset)
PORT=3001

# MongoDB connection string. Use 127.0.0.1 to prefer IPv4.
# For local MongoDB: mongodb://127.0.0.1:27017/medipredict
# For MongoDB Atlas, provide your connection string.
MONGO_URI=mongodb://127.0.0.1:27017/medipredict

# JWT signing secret. Use a strong, random value in production.
JWT_SECRET=replace-with-strong-random-secret

# Flask ML service URL. Default is http://127.0.0.1:5001
# Ensure the ML service is running and reachable.
ML_SERVICE_URL=http://127.0.0.1:5001

# Google Gemini API configuration for AI health insights.
# Obtain API key from Google AI Studio.
GEMINI_API_KEY=your-gemini-api-key
# Optional: preferred model name (falls back to working models if unavailable)
GEMINI_MODEL=gemini-2.0-flash

# =====================
# ML Service (Python/Flask)
# =====================
# Flask environment and port (if your ML service uses these)
FLASK_ENV=development
FLASK_PORT=5001

# Example additional ML settings (uncomment as needed):
# MODEL_DIR=ml/models
# DATA_DIR=ml/data

# =====================
# Node/NPM environment
# =====================
NODE_ENV=development

MODEL_PATH=models/model.pkl
SCALER_PATH=models/scaler.pkl
###############################################
# Notes
# - Do NOT commit real secrets. `.env` is ignored by .gitignore.
# - Update ports if you change defaults.
# - Ensure services are healthy via `/api/health` on the backend.
###############################################